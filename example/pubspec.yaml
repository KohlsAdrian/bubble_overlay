name: bubble_overlay_example
description: Demonstrates how to use the bubble_overlay plugin.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.0
  bubble_overlay:
    path: ../
    
flutter:
  assets:
    - assets/video.mp4
    - assets/video_vertical.mp4